{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window core APIs and an explicit workbook-open dialog.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
