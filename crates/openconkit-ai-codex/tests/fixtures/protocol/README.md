# Synthetic Codex protocol fixtures

These JSONL envelopes are synthetic and contain no captured account,
credential, prompt, workbook, or model data. They exercise the pinned
app-server envelope shapes that OpenConKit accepts and the server-initiated
request shape that it rejects. Update them only alongside the pinned schema
and transport contract tests.
